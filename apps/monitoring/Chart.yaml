apiVersion: v2
name: monitoring-stack
description: Prometheus + Grafana (kube-prometheus-stack) für ArgoCD Git-Sync
type: application
version: 0.1.0
appVersion: "1.0.0"

dependencies:
  - name: kube-prometheus-stack
    version: "65.5.1"    # ggf. auf aktuelle Version anpassen
    repository: "https://prometheus-community.github.io/helm-charts"
